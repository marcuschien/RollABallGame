using UnityEngine;
using System.Collections;

public class HelloWorld : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        print("\n Hello World!"); // This line is now ignored.
    }

    // Update is called once per frame
    void Update()
    {
        print("\n Hello World!");
    }
}